import 'package:flutter/material.dart';

/// ── Shared design tokens ─────────────────────────────────────────
/// Import this into every screen so colors stay consistent across
/// the app instead of being redefined per-file.
class LmsColors {
  static const bg = Color(0xFFF7F7F9);
  static const surface = Colors.white;
  static const border = Color(0xFFE7E7EC);
  static const primary = Color(0xFF3B6FE0);
  static const primarySoft = Color(0xFFE8EFFD);
  static const textDark = Color(0xFF15151C);
  static const textGrey = Color(0xFF8A8A96);
  static const success = Color(0xFF1E9E5A);
  static const error = Color(0xFFB42318);
  static const errorBg = Color(0xFFFDECEC);
  static const errorBorder = Color(0xFFF4B9B9);
}